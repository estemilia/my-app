/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    basePath: '/my-app',
    output: 'export',
    distDir: 'build',
    // trailingSlash: true,
}

module.exports = nextConfig
